{
  "name": "esg-blockchain-mvp",
  "version": "1.0.0",
  "description": "ESG Blockchain MVP - Complete Application",
  "scripts": {
    "start": "concurrently \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd backend && npm run dev",
    "start:frontend": "cd frontend && npm start",
    "install:all": "npm install && cd blockchain && npm install && cd ../backend && npm install && cd ../frontend && npm install && cd ..",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "blockchain:compile": "cd blockchain && npx hardhat compile",
    "blockchain:deploy": "cd blockchain && npx hardhat run scripts/deploy.js --network ganache",
    "dev": "npm run docker:up && timeout 10 && npm start",
    "setup": "npm run install:all && npm run docker:up && timeout 15 && npm run blockchain:compile && npm run blockchain:deploy"
  },
  "keywords": [
    "esg",
    "blockchain",
    "mvp"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "autoprefixer": "^10.4.21",
    "concurrently": "^8.2.2",
    "postcss": "^8.5.6",
    "tailwindcss": "^4.1.16"
  }
}
